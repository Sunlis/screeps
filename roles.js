var roles = {};

roles.harvester = require('role.harvester');
roles.tanker = require('role.tanker');
roles.builder = require('role.builder');
roles.guard = require('role.guard');
roles.repair = require('role.repair');

module.exports = roles;
